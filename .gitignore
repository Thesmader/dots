# OS
.DS_Store

# Migration env (contains paths to secrets)
.env.migration

# Secrets — never track
gh/.config/gh/hosts.yml
ssh/.ssh/id_*
ssh/.ssh/known_hosts

# Claude — ignore everything, then whitelist
claude/.claude/*
!claude/.claude/CLAUDE.md
!claude/.claude/settings.json
!claude/.claude/commands/
!claude/.claude/scripts/
claude/.claude/scripts/.usage-cache.json
!claude/.claude/skills/

**/.zcompdump
